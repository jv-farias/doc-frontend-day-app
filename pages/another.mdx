another.mdx
# Another Page

```js filename="demo.js" {3} copy
let a = 1;

console.log(a);
```

## Component

import { useState } from 'react'

{/* Import CSS modules */}
import styles from '../components/style.module.css'

export const Counter = () => {
  const [count, setCount] = useState(0);
  return (
    <div>
      <button onClick={() => setCount(count + 1)} className={styles.counter}>Clicked {count} times</button>
    </div>
  );
};

<Counter/>

## COMPONENTE ESTILO

import style from '../components/style.module.css'
import logoFrontEnd from '../../assets/logos/logo-frontend-ce.svg'



<div> 
  <span className={style.frontend}>FRONTEND </span><span className={style.day}>DAY</span>
</div>

<div>
   <img  className={style.logo} src={logoFrontEnd }/>    
</div>

## External Component

import Counters from '../components/counters'

<Counters />


import FrontEnd from '../components/frontend'

<FrontEnd />
